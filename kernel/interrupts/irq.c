/*
 * kernel/interrupts/irq.c
 *
 * Hardware IRQ handlers like timer, keyboard and future devices. Register the
 * appropriate callbacks here and forward events to the kernel subsystems.
 */

void irq_init(void) {}
