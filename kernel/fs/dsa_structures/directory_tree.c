/*
 * directory_tree.c
 *
 * N-ary tree representing directories and entries. Keep APIs like
 * `dir_create`, `dir_add_entry`, `dir_find()` and `dir_remove_entry`.
 */

#include <stdint.h>

void directory_tree_init(void) {}
