/*
 * kernel/drivers/timer.c
 *
 * Programmable Interval Timer driver used to generate periodic interrupts for
 * preemptive scheduling and timekeeping. Implement `pit_init` that sets
 * frequency and registers the timer IRQ.
 */

void pit_init(void) {}
